<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="top.whiteleaf03.api.mapper.UserInterfaceRecordMapper">
    <insert id="insert">
        INSERT INTO windy_api.user_interface_record (userId, interfaceInfoId, totalNum, leftNum)
            VALUE (#{userId}, #{interfaceInfoId}, #{totalNum}, #{leftNum});
    </insert>

    <select id="selectInterfaceInfoIdAndTotalNumAndLeftNumAndCreateTimeAndUpdateTime" resultType="top.whiteleaf03.api.modal.entity.UserInterfaceRecord">
        SELECT interfaceInfoId, totalNum, leftNum, updateTime
        FROM windy_api.user_interface_record
        WHERE userId = #{userId};
    </select>
</mapper>